---
- hosts: all
  name: Prepare
  pre_tasks:
  - apt:
      cache_valid_time: 86400
      update_cache: true
    name: Update the apt-cache, if necessary
    when: ansible_facts['os_family'] == 'Debian'
  roles:
  - buluma.setuptools
